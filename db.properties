# Database Configuration
# Replace these with your actual credentials or set via environment variables

# MySQL Connection Settings
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/eduinsight?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
db.user=${DB_USER:root}
db.password=${DB_PASSWORD:pun1613}

# Connection Pool Settings
db.max.connections=10
db.connection.timeout=30000
